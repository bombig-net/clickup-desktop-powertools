<Window x:Class="ClickUpDesktopPowerTools.UI.Overlay.TimeTrackingOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="268">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="28"/>
        </Grid.ColumnDefinitions>
        
        <!-- Content Card -->
        <Border x:Name="ContentCard"
                Grid.Column="0"
                Background="#CC1E1E1E"
                BorderBrush="#0DFFFFFF"
                BorderThickness="1,1,0,1"
                CornerRadius="10,0,0,10"
                MouseEnter="ContentCard_MouseEnter"
                MouseLeave="ContentCard_MouseLeave">
            <StackPanel Margin="14,12">
                <!-- Task Name -->
                <TextBlock x:Name="TaskNameText"
                           Text="Task Name"
                           Foreground="#B8B8B8"
                           FontSize="14"
                           FontWeight="Medium"
                           TextWrapping="Wrap"
                           TextTrimming="CharacterEllipsis"
                           MaxHeight="36">
                    <TextBlock.LineHeight>18</TextBlock.LineHeight>
                </TextBlock>
                
                <!-- Time Display -->
                <TextBlock x:Name="TimeText"
                           Text="Session: 00:00h / Total: 00:00"
                           Foreground="#676767"
                           FontSize="13"
                           FontWeight="Normal"
                           Margin="0,4,0,0">
                    <TextBlock.LineHeight>16</TextBlock.LineHeight>
                </TextBlock>
            </StackPanel>
        </Border>
        
        <!-- Handle -->
        <Border x:Name="HandleBorder"
                Grid.Column="1"
                Background="#CC1E1E1E"
                BorderBrush="#0DFFFFFF"
                BorderThickness="1,1,1,1"
                CornerRadius="0,0,0,0"
                MouseRightButtonUp="Handle_MouseRightButtonUp">
            <Border.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Stop Timer" Click="StopTimer_Click"/>
                    <MenuItem Header="Open Task" Click="OpenTask_Click"/>
                </ContextMenu>
            </Border.ContextMenu>
            
            <!-- Three Dots Icon -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse Width="3" Height="3" Fill="#676767" Margin="0,0,0,4"/>
                <Ellipse Width="3" Height="3" Fill="#676767" Margin="0,0,0,4"/>
                <Ellipse Width="3" Height="3" Fill="#676767"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

